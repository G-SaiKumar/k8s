apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
  namespace: learn-k8s
spec:
  # list of containers
  containers:
  - name: hello-pod
    image: nginx
    #here with this line port will not be opened, just for information
    ports:
    - containerPort: 80